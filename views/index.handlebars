<h2>News Scrape!</h2>
<h4>A resource site for all of your news!</h4>

<div id="articles">
    {{!-- this isn't working because I'm not returning the JSON as an object like I did with project 2
    Look at the burger homework instead. --}}
    <p>This is where the users cluster will be displayed. </p>
    {{#each articles}}
    <br>
    <div data-id={{_id}}>
        <a href={{link}}>{{title}}</a>
        <p>{{comment}}</p>
        <button id="addComment">Add Comment</button>
        <div id="comments">
            {{!-- Textarea will appear --}}
            {{!-- Submit button will be here --}}
        </div>
    </div>
    {{/each}}
</div>

{{!-- <style>
     #articles {
        display: inline-block;
        overflow: auto;
        width: 60%;
        height: 700px;
      }
      #notes {
        float: right;
        width: 30%;
      }
</style> --}}

<script src="/assets/js/app.js" type="text/javascript"></script>